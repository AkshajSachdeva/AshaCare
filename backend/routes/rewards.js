import { Router } from 'express'; import { RewardTransaction, User } from '../models/index.js'; import { auth } from '../middleware/auth.js'; const router=Router();router.use(auth);
router.get('/',async(req,res)=>{const leaderboard=await User.find({role:'asha_worker'}).select('fullName totalPoints assignedRegion').sort({totalPoints:-1,fullName:1});const transactions=await RewardTransaction.find({workerId:req.user._id}).sort({createdAt:-1}).limit(20);const rank=leaderboard.findIndex(x=>String(x._id)===String(req.user._id))+1;res.json({success:true,message:'Rewards loaded',data:{totalPoints:req.user.totalPoints,transactions,leaderboard:leaderboard.map((u,i)=>({...u.toObject(),rank:i+1})),rank:rank||null,modelLabel:'AshaCare incentive allocation model — hackathon demo'}})});export default router;

