import { useMemo, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { mockSchemes } from "../mockData/schemes";
import { useLanguage } from "../i18n";
const readableCategory = (category) => category.replaceAll("_", " ");
const SchemeRegistrationPage = () => {
 const { schemeId } = useParams(); const navigate = useNavigate(); const { t } = useLanguage(); const scheme = mockSchemes.find((item) => item._id === schemeId);
 const [patientId] = useState(""); const [patient] = useState(null); const [recommendedSchemes] = useState(mockSchemes);
 const [form, setForm] = useState({ patientName: patient?.fullName || "", age: patient?.age || "", village: patient?.village || patient?.address || "", healthCategory: patient?.healthCategories?.[0] || scheme?.applicableCategories?.[0] || "", contactNumber: patient?.phoneNumber || "", declaration: false });
 const [checkedDocuments, setCheckedDocuments] = useState([]); const [registration, setRegistration] = useState(null);
 const isReady = form.patientName.trim() && form.age && form.healthCategory && form.declaration && checkedDocuments.length === (scheme?.requiredDocuments.length || 0);
 const demoReferenceNumber = useMemo(() => `ASC-${Date.now().toString().slice(-6)}-${Math.random().toString(36).slice(2, 6).toUpperCase()}`, []);
 if (!scheme) return <div className="mx-auto max-w-3xl px-4 py-10 sm:px-6"><section className="rounded-2xl border border-slate-200 bg-white p-6 text-center shadow-sm"><h1 className="text-2xl font-bold text-slate-900">{t.schemeNotFound}</h1><Link to="/schemes" className="mt-5 inline-block rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white">{t.backToSchemes}</Link></section></div>;
 const updateForm = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.type === "checkbox" ? event.target.checked : event.target.value }));
 const toggleDocument = (document) => setCheckedDocuments((current) => current.includes(document) ? current.filter((item) => item !== document) : [...current, document]);
 const submitRegistration = (event) => { event.preventDefault(); if (!isReady) return; const nextRegistration = { referenceNumber: demoReferenceNumber, status: "registration_started", patientId, schemeId: scheme._id, patientName: form.patientName, submittedAt: new Date().toISOString() }; const registrations = JSON.parse(localStorage.getItem("mockSchemeRegistrations") || "[]"); localStorage.setItem("mockSchemeRegistrations", JSON.stringify([...registrations.filter((item) => item.schemeId !== scheme._id), nextRegistration])); setRegistration(nextRegistration); };
 if (registration) return <div className="mx-auto max-w-3xl px-4 py-10 sm:px-6"><section className="rounded-2xl border border-emerald-200 bg-white p-6 shadow-sm sm:p-8"><span className="inline-flex rounded-full bg-emerald-100 px-3 py-1 text-sm font-bold text-emerald-800">{t.registrationStarted}</span><h1 className="mt-4 text-3xl font-bold text-slate-900">{t.registrationSuccess}</h1><p className="mt-3 text-slate-600">{t.registrationRecorded.replace("{schemeName}", scheme.schemeName)}</p><dl className="mt-6 rounded-xl bg-emerald-50 p-4"><div><dt className="text-sm font-semibold text-emerald-900">{t.referenceNumber}</dt><dd className="mt-1 text-xl font-bold text-emerald-800">{registration.referenceNumber}</dd></div><div className="mt-4"><dt className="text-sm font-semibold text-emerald-900">{t.patientName}</dt><dd className="mt-1 font-bold text-emerald-800">{registration.patientName}</dd></div><div className="mt-4"><dt className="text-sm font-semibold text-emerald-900">{t.registrationDate}</dt><dd className="mt-1 font-bold text-emerald-800">{new Intl.DateTimeFormat(t === undefined ? "en-IN" : undefined, { dateStyle: "medium" }).format(new Date(registration.submittedAt))}</dd></div><div className="mt-4"><dt className="text-sm font-semibold text-emerald-900">{t.registrationStatus}</dt><dd className="mt-1 font-bold text-emerald-800">{registration.status}</dd></div></dl><div className="mt-7 flex flex-wrap gap-3"><Link to="/schemes" className="rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white hover:bg-emerald-800">{t.backToSchemes}</Link><Link to={`/schemes/${scheme._id}`} className="rounded-xl border border-emerald-700 px-5 py-3 font-bold text-emerald-800 hover:bg-emerald-50">{t.viewRegistration}</Link></div></section></div>;
 return <div className="mx-auto max-w-3xl px-4 py-6 sm:px-6 lg:px-8"><button type="button" onClick={() => navigate(`/schemes/${scheme._id}`)} className="text-sm font-semibold text-emerald-800 underline">← {t.backToDetails}</button><header className="mt-5"><p className="text-sm font-semibold uppercase tracking-wider text-emerald-700">{t.schemeRegistration}</p><h1 className="mt-1 text-3xl font-bold text-slate-900">{t.registerFor} {scheme.schemeName}</h1><p className="mt-2 text-slate-600">{t.registrationDescription}</p></header><form onSubmit={submitRegistration} className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700">{t.patientName}<input required name="patientName" value={form.patientName} onChange={updateForm} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-slate-900" /></label><label className="text-sm font-semibold text-slate-700">{t.age}<input required min="0" type="number" name="age" value={form.age} onChange={updateForm} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-slate-900" /></label><label className="text-sm font-semibold text-slate-700">{t.villageArea} <span className="font-normal text-slate-500">{t.ifAvailable}</span><input name="village" value={form.village} onChange={updateForm} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-slate-900" /></label><label className="text-sm font-semibold text-slate-700">{t.healthCategory}<select required name="healthCategory" value={form.healthCategory} onChange={updateForm} className="mt-2 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-slate-900">{scheme.applicableCategories.map((category) => <option key={category} value={category}>{readableCategory(category)}</option>)}</select></label><label className="text-sm font-semibold text-slate-700 sm:col-span-2">{t.contactNumber}<input name="contactNumber" inputMode="tel" value={form.contactNumber} onChange={updateForm} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-slate-900" /></label></div><fieldset className="mt-7"><legend className="text-lg font-bold text-slate-900">{t.requiredDocuments}</legend><p className="mt-1 text-sm text-slate-600">{t.documentsHelp}</p><div className="mt-4 space-y-3">{scheme.requiredDocuments.map((document) => <label key={document} className="flex items-center gap-3 rounded-xl border border-slate-200 p-3 text-slate-700"><input type="checkbox" checked={checkedDocuments.includes(document)} onChange={() => toggleDocument(document)} className="h-4 w-4 accent-emerald-700" />{document}</label>)}</div></fieldset><label className="mt-7 flex gap-3 rounded-xl bg-emerald-50 p-4 text-sm text-slate-700"><input required type="checkbox" name="declaration" checked={form.declaration} onChange={updateForm} className="mt-0.5 h-4 w-4 accent-emerald-700" /><span>{t.declaration}</span></label><button type="submit" disabled={!isReady} className="mt-7 w-full rounded-xl bg-emerald-700 px-5 py-3.5 text-lg font-bold text-white hover:bg-emerald-800 disabled:cursor-not-allowed disabled:bg-emerald-200 disabled:text-emerald-800">{t.submitRegistration}</button></form></div>;
};
export default SchemeRegistrationPage;
