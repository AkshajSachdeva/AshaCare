import{Bell,CalendarDays,HeartPulse,MapPin,Star,Users}from'lucide-react';import{useState}from'react';import{Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{useI18n}from'../i18n/I18nContext';import{api}from'../lib/api';import{ErrorState,Loading,useLoad}from'../components/UI';
export default function Home(){const{user}=useAuth(),{t,language}=useI18n(),{data,error,loading,reload}=useLoad(()=>api('/dashboard'),[]),[selected,setSelected]=useState(null);if(loading)return <Loading/>;if(error)return <ErrorState message={error} onRetry={reload}/>;const now=new Date(),days=new Date(now.getFullYear(),now.getMonth()+1,0).getDate(),first=new Date(now.getFullYear(),now.getMonth(),1).getDay();const stats=[[t('visitsToday'),data.stats.visitsToday,CalendarDays,'bg-blue-50 text-blue-600'],[t('highRisk'),data.stats.highRisk,HeartPulse,'bg-red-50 text-red-600'],[t('followUpsDue'),data.stats.followUpsDue,Users,'bg-amber-50 text-amber-600'],[t('totalPoints'),data.stats.totalPoints,Star,'bg-care-50 text-care-600']];return <><header className="bg-gradient-to-br from-care-700 to-care-500 px-5 pb-20 pt-7 text-white"><div className="flex items-start justify-between"><div><p className="text-sm text-white/75">{t('goodMorning')}</p><h1 className="text-2xl font-black">{user.fullName.split(' ')[0]} 👋</h1><p className="mt-1 flex items-center gap-1 text-xs text-white/70"><MapPin size={13}/>{user.assignedRegion}</p></div><Link to="/notifications" className="grid h-11 w-11 place-items-center rounded-2xl bg-white/15"><Bell size={20}/></Link></div></header><div className="-mt-14 px-5"><div className="grid grid-cols-2 gap-3">{stats.map(([label,value,Icon,style])=><div className="card p-4" key={label}><div className={`grid h-9 w-9 place-items-center rounded-xl ${style}`}><Icon size={18}/></div><strong className="mt-3 block text-2xl">{value}</strong><span className="text-xs font-semibold text-slate-500">{label}</span></div>)}</div><section className="card mt-5 p-5"><div className="flex items-end justify-between"><div><h2 className="section-title">{t('careCalendar')}</h2><p className="text-xs text-slate-400">{now.toLocaleDateString(language,{month:'long',year:'numeric'})} · {t('workload')}</p></div><div className="flex gap-1">{[0,1,2,3,4].map(x=><i key={x} className={`h-3 w-3 rounded-sm ${['bg-care-50','bg-care-100','bg-care-200','bg-care-400','bg-care-700'][x]}`}/>)}</div></div><div className="mt-5 grid grid-cols-7 gap-1.5 text-center text-[10px] font-bold text-slate-400">{['S','M','T','W','T','F','S'].map((d,i)=><span key={i}>{d}</span>)}{Array.from({length:first}).map((_,i)=><span key={`e${i}`}/>)}{Array.from({length:days}).map((_,i)=>{const date=new Date(now.getFullYear(),now.getMonth(),i+1),key=date.toISOString().slice(0,10),entry=data.calendar[key],level=Math.min(entry?.visits||0,4),today=i+1===now.getDate();return <button key={key} onClick={()=>setSelected({day:i+1,...(entry||{visits:0,followUps:0,patients:[]})})} className={`aspect-square rounded-lg text-xs font-black transition active:scale-90 ${['bg-care-50 text-slate-400','bg-care-100 text-care-700','bg-care-200 text-care-700','bg-care-400 text-white','bg-care-700 text-white'][level]} ${today?'ring-2 ring-care-yellow ring-offset-2':''}`}>{i+1}</button>})}</div>{selected&&<div className="mt-4 rounded-2xl bg-care-50 p-4"><div className="flex justify-between"><strong>{now.toLocaleDateString(language,{month:'short'})} {selected.day}</strong><button className="text-xs font-bold text-care-600" onClick={()=>setSelected(null)}>Close</button></div><p className="mt-1 text-sm">{selected.visits} {t('plannedVisits')} · {selected.followUps} {t('followUps')}</p>{selected.patients.length>0&&<p className="mt-1 text-xs text-slate-500">{selected.patients.join(', ')}</p>}</div>}</section><div className="mt-5 flex gap-3"><Link to="/patients?add=1" className="btn-primary flex-1">+ {t('addPatient')}</Link><Link to="/patients" className="btn-secondary flex-1">{t('patients')}</Link></div></div></>}

